<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rosaguadalupe.diseo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="DisxApp"
        android:roundIcon="@mipmap/logo"
        android:screenOrientation="portrait"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:background="@mipmap/background">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activityPuntaje" />
        <activity android:name=".activityMenuJuegos" />
        <activity android:name=".activityPuntajeActualHombre" />
        <activity android:name=".activityPuntajeActualMujer" />
        <activity android:name=".activitySabiasQue" />
        <activity android:name=".activityDiagnostico" />
        <activity android:name=".activityOraciones"></activity>
    </application>

</manifest>